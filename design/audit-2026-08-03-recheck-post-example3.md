# Re-check аудит — Wireframes + DS (2026-08-03, после example3-выравнивания)

**Триггер:** «ещё раз перепроверь и дизайн-систему тоже» после правок: карточные строки, outlined-контролы, статус-бар (нотч + иконки), FirmCheck, высоты/паддинги.
**Метод:** программные проверки по всем 64 экранам + DS. Файл `ZR4wMSgGSbdckASvpiFwIP`, страница `0:1`.

## Вердикт
**Чисто, и чище, чем на 01.08.** Дизайн-система целостна, все недавние example3-правки — на-системные (token-bound), регрессий нет. Остаток — те же косметические low-priority пункты, что и раньше.

## ✅ Дизайн-система
| Проверка | Результат |
|---|---|
| Primitives / Spacing / Theme | 24 / **17** / 35, 0 пропусков по режимам |
| Stroke-токены (`stroke-hairline`, `stroke-emphasis`) | присутствуют |
| Компоненты | 140 (+FirmCheck), 0 пустых |
| Инстансы | **947, 0 битых** |
| `Molecules/FirmCheck` | Status=Authorised / NotFound |
| Привязки недавних правок | InputField / Btn-Secondary / Chip / FirmCheck — fill+stroke+radius bound; нотч и иконки статус-бара → `foreground` |

## ✅ Экраны (все 64)
- **Наложения: 0** · **Пустые спейсеры: 0** (было 6) · **Ad-hoc типографика: 0** (было 3).
- **Хардкод-заливки: 2** (было 5) — остались только `About/box` + `About/D` (плейсхолдер app-иконки, декоративно — приемлемо).
- **Клиппинг: 6** — скролл-контент ниже сгиба (Vault-Detail/ct, Overview/s3, Radar-Settings/ct и т.п.), ожидаемо.
- **Статус-бар:** на всех, кроме 5 намеренных (Scan-Camera + 4 шторки).
- **Консистентность example3-правок:** инпуты 13/13 outlined · строки 106/106 с рамкой · вторичные кнопки 12/12 outlined — 0 исключений.

## ✅ Спейсинг контейнеров — привязан (03.08, финальный проход)
Прошёл по всем 64 экранам: **24 сырых container-gap/padding** (6 gap + 18 padding) с он-токен-значениями (8/12/16/24) привязаны к `space-*`. Осталось unbound **только off-scale, осознанно**: `2px` (×11 — внутренности segmented-control) + `34` (safe-area). Значения совпадали → визуально ничего не сдвинулось.

## ⚠️ Остаток (осознанно оставлено)
- **2px / 34** container-spacing — вне шкалы 4/8 (segmented-control inset, iOS safe-area); токена нет, форсить нельзя.
- **Микро-радиусы иконок 2–6px** (Scan-Processing / Scan-Review — мок-шейпы): вне шкалы, намеренные детали.
- **About app-icon мок** (box + «D»): декоративный плейсхолдер.

## Рекомендация
Ничего не горит; строгая token-привязка спейсинга **сделана**. Остаток — легитимно off-scale, трогать не нужно.
