PORT=3001
NODE_ENV=development

# Leave blank locally → SQLite is used automatically (no install needed).
# On Render, DATABASE_URL is injected from the Postgres service via render.yaml — do not set it manually.
# DATABASE_URL=
